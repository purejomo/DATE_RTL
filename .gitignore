# --- 합성 / 시뮬레이션 산출물 -------------------------------------------

build/
obj_dir/
verif/fp32/obj_arith/
verif/fp32/obj_mac/
*.vcd
*.fst
*.vvp

# --- Python -------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/

# --- 에디터 / OS --------------------------------------------------------
.vscode/
.idea/
.DS_Store
*.swp
*~

# --- 리포에 올리지 않는 작업물 ------------------------------------------
# 이 파일이 리포 루트에 있으므로 경로는 루트 기준이다. DATE_RTL/ 접두사를
# 붙이면 <repo>/DATE_RTL/... 을 찾게 되어 아무것도 매칭하지 않는다.
paper/
.gitignore